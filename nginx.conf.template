server {
  listen 0.0.0.0:${PORT};
  server_name _;

  # Healthcheck
  location /healthz {
    return 200 "ok\n";
  }

  # Teste direto SEM AUTH, SEM HEADER, SEM TLS CHECK
  location /http/proxy {
    proxy_pass ${CPI_URL};
    proxy_ssl_verify off;
  }
}
